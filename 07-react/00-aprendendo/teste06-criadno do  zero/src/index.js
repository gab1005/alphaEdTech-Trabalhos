import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

const gab = ReactDOM.createRoot(document.getElementById('root'));

gab.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>
);