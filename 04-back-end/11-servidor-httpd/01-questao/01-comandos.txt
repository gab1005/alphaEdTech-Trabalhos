//verificar entre os serviços se o apache está rodando
ps -ef | grep apache

//verifica a versão do apache2
apache2 -v

//atualizar sistema
sudo apt-get update

//para para a aplicação
/etc/init.d/apache2 stop

//verificar ele no sistema
ps -ef | grep apache

//para remover todo que tenha conexão com apache
sudo apt-get remove apache*

//naão consegui apagar de primeira a pasta /etc/apache2/ e os  arquivos dentro dela, logo tentei outros comandos como:

sudo apt-get remove apache2

sudo apt-get autoremove apache2

sudo apt-get --purge remove apache2

//por fim acabei apagando manualmente as pastas usando  rm e rmdir, ate quem não exitisse a pasta apach2
